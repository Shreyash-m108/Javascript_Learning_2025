let person1 = {
    fname : "Shreyash",
    lname : "Magdum",
    height : 5.6,
    hobby : "Gaming",
    isMarried : false
}

// console.log(person1);

if(person1.isMarried === true)
        console.log(`${person1.fname} is married.`);
else
        console.log(`${person1.fname} is not married.`);